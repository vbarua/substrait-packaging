# substrait-antlr

Used to generate new Python ANTLR parser bindings for Substrait.

## Generating Locally
The `generate_antlr.sh` script can be executed locally to check the generation.

Set `SUBSTRAIT_HOME` to a directory containing the Substrait specification
